-- File generated by genmodules.sh. Do not modify.
{-# LANGUAGE AllowAmbiguousTypes #-}
{-# LANGUAGE UndecidableInstances #-}

{-# OPTIONS_GHC -Wno-orphans #-}
{-# OPTIONS_HADDOCK not-home #-}

module I.Generated.CInt () where

import Control.Monad
import Data.Constraint
import Data.Int
import Data.Maybe
import Data.Proxy
import Data.Type.Ord
import Foreign.C.Types
import KindInteger (type (/=), type (==))
import KindInteger qualified as K
import Prelude hiding (min, max, div)
import Prelude qualified as P

import I.Internal

--------------------------------------------------------------------------------

-- | This is so that GHC doesn't complain about the unused modules,
-- which we import here so that `genmodules.sh` doesn't have to add it
-- to the generated modules.
_ignore :: (CSize, Int)
_ignore = (0, 0)

--------------------------------------------------------------------------------

type instance MinL CInt = MinT CInt
type instance MaxR CInt = MaxT CInt

instance forall (l :: K.Integer) (r :: K.Integer).
  ( IntervalCtx CInt l r
  ) => Interval CInt l r where
  type IntervalCtx CInt l r =
    ( K.KnownInteger l
    , K.KnownInteger r
    , MinT CInt <= l
    , l <= r
    , r <= MaxT CInt )
  type MinI CInt l r = l
  type MaxI CInt l r = r

instance
  ( Interval CInt l r, InhabitedCtx CInt l r
  ) => Inhabited CInt l r where
  inhabitant = min
  from = \x -> UnsafeI x <$ guard (l <= x && x <= r)
    where l = fromInteger (K.integerVal (Proxy @l)) :: CInt
          r = fromInteger (K.integerVal (Proxy @r)) :: CInt

  negate' (unwrap -> x) = do
    guard (x /= minBound)
    from (P.negate x)

  (unwrap -> a) `plus'` (unwrap -> b)
    | b > 0 && a > maxBound - b = Nothing
    | b < 0 && a < minBound - b = Nothing
    | otherwise                 = from (a + b)

  (unwrap -> a) `mult'` (unwrap -> b) = do
    guard $ case a <= 0 of
      True  | b <= 0    -> a == 0 || b >= (maxBound `quot` a)
            | otherwise -> a >= (minBound `quot` b)
      False | b <= 0    -> b >= (minBound `quot` a)
            | otherwise -> a <= (maxBound `quot` b)
    from (a * b)

  (unwrap -> a) `minus'` (unwrap -> b)
    | b > 0 && a < minBound + b = Nothing
    | b < 0 && a > maxBound + b = Nothing
    | otherwise                 = from (a - b)

  (unwrap -> a) `div'` (unwrap -> b) = do
    guard (b /= 0 && (b /= -1 || a /= minBound))
    let (q, m) = divMod a b
    guard (m == 0)
    from q

instance (Inhabited CInt l r) => Clamp CInt l r

instance (Inhabited CInt ld rd, Inhabited CInt lu ru, lu <= ld, rd <= ru)
  => Up CInt ld rd lu ru

instance forall l r t.
  ( Inhabited CInt l r, KnownCtx CInt l r t
  ) => Known CInt l r t where
  type KnownCtx CInt l r t = (K.KnownInteger t, l <= t, t <= r)
  known' = UnsafeI . fromInteger . K.integerVal

instance forall l r. (Inhabited CInt l r) => With CInt l r where
  with x g
    | K.SomeInteger (pt :: Proxy t) <- K.someIntegerVal (toInteger (unwrap x))
    = fromMaybe (error "I.with: impossible") $ do
        Dict <- leInteger @l @t
        Dict <- leInteger @t @r
        pure (g pt)

instance (Inhabited CInt l r, l /= r) => Discrete CInt l r where
  pred' i = UnsafeI (unwrap i - 1) <$ guard (min < i)
  succ' i = UnsafeI (unwrap i + 1) <$ guard (i < max)

instance (Zero CInt l r, l == K.Negate r) => Negate CInt l r where
  negate = UnsafeI . P.negate . unwrap

instance (Inhabited CInt l r, l <= K.P 0, K.P 0 <= r) => Zero CInt l r where
  zero = UnsafeI 0

instance (Inhabited CInt l r, l <= K.P 1, K.P 1 <= r) => One CInt l r where
  one = UnsafeI 1

instance forall l r. (Inhabited CInt l r) => Shove CInt l r where
  shove = \x -> fromMaybe (error "shove(CInt): impossible") $
                  from $ fromInteger (mod (toInteger x) (r - l + 1) + l)
    where l = toInteger (unwrap (min @CInt @l @r))
          r = toInteger (unwrap (max @CInt @l @r))

